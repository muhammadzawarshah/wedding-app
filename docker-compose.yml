services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: wedding
      POSTGRES_USER: wedding
      POSTGRES_PASSWORD: wedding
    ports:
      - "5432:5432"
    volumes:
      - wedding_postgres:/var/lib/postgresql/data
volumes:
  wedding_postgres:
